<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ca.usherbrooke.gegi.server.persistence.SaleMapper">

    <select id="select" resultType="Sale">
      SELECT
        id_sale,
        sale_title,
        sale_description,
        sale_date,
        sale_price,
        sale_wear,
        sale_nego,
        sale_location
      FROM
        nouvelusage.sale
      where
        id_sale = #{id};

    </select>
</mapper>
